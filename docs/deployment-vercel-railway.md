# Vercel + Railway Deployment Guide

The canonical V1 production demo deployment guide is now:

```text
docs/deployment.md
```

Use that file for the OpenRouter, Upstash Redis, Vercel, Railway orchestrator, Railway Mock IdP, and Railway external connector agent configuration. This pointer remains only for older links.
